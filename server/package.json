{
  "name": "aquaconnect-server",
  "version": "1.0.0",
  "private": true,
  "description": "AquaConnect API — Express + Railway Postgres backend (auth, farms, memos, reports, share links, NIFS ocean-temp proxy)",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "smoke-test": "node scripts/smoke_test.mjs"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "dotenv": "^16.4.7",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "pg-mem": "^3.0.4"
  }
}
